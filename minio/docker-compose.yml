services:
  minio:
    image: quay.io/minio/minio
    container_name: minio
    ports:
      - "9000:9000"
      - "9090:9090"  
    environment:
      MINIO_ROOT_USER: t0Ocqct2c52v0NjIWFmC  # نام کاربری
      MINIO_ROOT_PASSWORD: D60M2JW4YmxSm4hmmL3kjjdsqlA6UF4SsNoQhYhm  # رمز عبور
    command: server /data --console-address ":9090"
    volumes:
      - minio_data:/data   # نام درست volume
    networks:
      - my_cdn  

volumes:
  minio_data:
    driver: local   # 👈 اینجا درست شد

networks:
  my_cdn:  # تعریف شبکه
    driver: bridge
